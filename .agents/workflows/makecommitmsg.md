---
description: make git commit summary following conventionalcommits specs
---

make a git commit summary and description based on the current uncommitted change of this project, strictly following the [git-commit skill](../skills/git-commit/SKILL.md) and https://www.conventionalcommits.org/en/v1.0.0. Commit messages must be written in plain text inside a ```commit codeblock and MUST NEVER contain agent-style markdown links (e.g. [file](file:///...)), URLs, or absolute paths. And if user asks to commit now, do the commit